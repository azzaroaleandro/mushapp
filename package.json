{
  "name": "mushapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Condizioni per cercare funghi in Trentino ed Emilia-Romagna",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "start": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1"
  },
  "engines": {
    "node": ">=22"
  }
}
